<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pj.vegi.search.mapper.NaverSearchMapper">

	<select id="getMaterialList" resultType="MaterialVo">
		select MAT_NAME
		from recipe_material
		where r_id = #{rId}
	</select>
	
	<!-- <select id="recipeMaterialSelect" resultType="MaterialVo">
		select
	</select> -->
</mapper>